image: alpine/edge
packages:
  - meson
  - samurai
  - sqlite-dev
  - util-linux-dev  
tasks:
  - build: |
      cd zkc
      meson build
      samu -C build
sources:
  - https://git.sr.ht/~fancycade/zkc
